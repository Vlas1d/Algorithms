import * as HashMap from 'hashmap';

const hash_map = new HashMap();
const map_ = new Map();

for (let i = 0; i < 1000; i++) {
    map.set(Math.floor(Math.random() * 100), Math.floor(Math.random() * 100))
    //hash_map.set(Math.floor(Math.random() * 100), Math.floor(Math.random() * 100))
}
